# 수정 보완 Implementation Plan

## Summary
- 메인 화면, 대시보드, 작업 영상, 커뮤니티를 발표용 완성도 기준으로 보완한다.
- 메인 화면은 `파이프 TIG 용접 훈련 허브` 제목을 안정적으로 표시하고, 5개 진입 버튼을 아이콘 기반 버튼으로 재구성한다.
- 대시보드는 복잡한 지표와 요약 패널을 제거하고, 마스터-카피 소개 중심의 현대적인 안내 화면으로 정리한다.
- 영상 데이터는 삭제된 것이 아니라 API 미실행 또는 일부 MP4 파일 미연결 때문에 안 보이는 상태다.
- 커뮤니티는 백엔드 없이 네이버 카페형 더미 게시판 UI로 구성한다.

## Key Changes
- 메인 페이지는 기존 4개 아이콘과 새 `community_icon.png`를 사용해 `숙련공 노하우 업로드`, `작업 영상`, `노하우 챗봇`, `사진 피드백`, `커뮤니티` 버튼을 구성한다.
- 대시보드는 서비스 소개, 핵심 기능 3개, 발표용 사용 흐름, 주요 CTA만 남긴다.
- 작업 영상은 재생 가능한 영상만 보여주지 않고 전체 메타데이터를 보여주며, 재생 가능 항목을 우선 정렬한다.
- 연결된 영상 카드는 실제 video URL의 `#t=1` 프레임을 썸네일처럼 표시한다.
- 커뮤니티는 카페명, 검색창, 카테고리 탭, 글쓰기 버튼, 게시글 목록, 인기글/최신 댓글/활동 현황 위젯을 제공한다.

## Test Plan
- `npm.cmd exec tsc -- --noEmit -p tsconfig.json`로 타입 검증한다.
- `npm.cmd run dev -- -p 3010`으로 프론트를 실행해 `/`, `/dashboard`, `/videos`, `/community`를 확인한다.
- API 서버가 켜진 상태에서 `curl http://localhost:8000/api/health`와 `/api/training-videos?material=stainless&position=6G`를 확인한다.
- `/videos`에서 영상 준비됨/파일 미연결 상태와 실제 영상 프레임 썸네일을 확인한다.
- 좁은 화면에서 메인 버튼, 대시보드 카드, 커뮤니티 게시글 목록이 겹치지 않는지 확인한다.

## Assumptions
- 커뮤니티는 이번 단계에서 UI 목업만 구현한다.
- 영상 재생은 FastAPI 서버 `localhost:8000`이 켜져 있어야 정상 동작한다.
- `ffmpeg`가 현재 PATH에 없으므로 정적 썸네일 생성 대신 브라우저 video frame 미리보기를 사용한다.
