# Git 업로드 안내

이 폴더는 GitHub 업로드용으로 정리한 버전입니다.

- `.env`, `node_modules`, `.next`, 업로드 임시 파일은 제외했습니다.
- 대용량 영상 파일(`*.mp4`)은 제외했습니다.
- 영상은 별도 Google Drive/Release/zip으로 공유하고, 필요 시 `apps/api/dataset/video`에 넣으면 됩니다.
- GCP 설정은 각자 `apps/api/rag_pipeline/.env.example`을 복사해 `.env`로 만든 뒤 입력합니다.

실행 명령은 `실행명령어.md` 또는 `팀원_실행가이드.md`를 참고하세요.
