# 마스터-카피 최종 웹 구현 팀원 실행가이드

이 폴더는 최종 구현물을 공유하기 위한 전달용 패키지입니다.

## 포함된 것

- `apps/web`: Next.js 프론트엔드 최종 구현
- `apps/api`: FastAPI 백엔드, RAG 데이터셋, 영상 매핑, 업로드/챗봇/피드백 API
- `apps/api/dataset/video`: 영상 메타/README 포함. Git 업로드본에서는 대용량 mp4 제외
- `apps/api/dataset/source_raw/Chatbot_DataSet`: 원본 챗봇 데이터셋 보관본(`def.md`, `qna.md`, 예비/질문 이미지). 대용량 mp4는 중복 방지를 위해 `dataset/video`에서 관리
- `scripts`: API 설치/실행/검증 PowerShell 스크립트
- `docs`: API 계약, 구조 문서, 구현 계획
- `reference_designs`: Stitch/Claude 참고 디자인 자료

## 제외된 것

- `node_modules`
- `.next`
- `.env`
- Python/Node 캐시
- 업로드 임시 파일
- 개인 GCP 인증 파일

개인 인증값은 공유하지 않습니다. 각 팀원은 본인 환경에서 `.env`와 GCP 인증을 설정해야 합니다.

## 1. API 실행

새 CMD 또는 PowerShell에서:

```bat
cd <최종_웹구현_전달용 폴더 경로>
npm.cmd run setup:api
```

GCP Vertex AI 설정 파일 생성:

```bat
copy apps\api\rag_pipeline\.env.example apps\api\rag_pipeline\.env
notepad apps\api\rag_pipeline\.env
```

`.env` 예시:

```env
GCP_PROJECT_ID=본인-gcp-project-id
GCP_LOCATION=asia-northeast3
VERTEX_MODEL=gemini-2.5-flash
```

GCP 인증:

```bat
gcloud auth application-default login
gcloud config set project 본인-gcp-project-id
gcloud auth application-default set-quota-project 본인-gcp-project-id
gcloud services enable aiplatform.googleapis.com --project 본인-gcp-project-id
```

API 실행:

```bat
npm.cmd run dev:api
```

확인:

```text
http://localhost:8000/api/health
```

정상 예시:

```json
{
  "status": "ok",
  "llm": {
    "configured": true,
    "provider": "vertex",
    "model": "gemini-2.5-flash"
  }
}
```

## 2. 웹 실행

API 터미널은 켜둔 상태에서 새 CMD 또는 PowerShell을 엽니다.

```bat
cd <최종_웹구현_전달용 폴더 경로>
npm.cmd run setup:web
npm.cmd run dev -- -p 3010
```

접속:

```text
http://localhost:3010
```

## 3. 평가 시연 포인트

### 실제 백엔드 연동 확인

챗봇에 질문을 보내면 API 터미널에 다음 로그가 찍힙니다.

```text
POST /api/answer HTTP/1.1 200 OK
```

웹 챗봇 화면에도 `실시간 백엔드 연동 로그` 패널이 표시됩니다.

확인할 항목:

- `POST /api/answer`
- 상태 코드 `200`
- provider `vertex`
- model `gemini-2.5-flash`

### 영상 연동 확인

Git 업로드본은 mp4를 제외했으므로, 영상 파일을 `apps/api/dataset/video`에 넣은 뒤 작업 영상 페이지에서 스테인리스 / 6G를 선택하면 연결된 mp4 영상이 재생됩니다.

### RAG 노하우 확인

챗봇 답변 아래에 다음 근거가 표시됩니다.

- 참고한 노하우
- 연결된 작업 영상
- 출처 정보

### 사진 피드백 확인

사진 피드백 페이지에서 이미지 업로드 후 피드백을 실행하면 `/api/upload`, `/api/feedback` 백엔드 요청이 발생합니다. `/api/feedback`은 `upload_id`로 저장된 이미지를 찾아 Vertex/Gemini 이미지+RAG 피드백을 생성합니다.

## 4. 자주 나는 오류

### `Missing script: dev`

반드시 전달용 루트 폴더에서 실행합니다.

```bat
cd <최종_웹구현_전달용 폴더 경로>
npm.cmd run dev -- -p 3010
```

### `EADDRINUSE`

이미 해당 포트가 실행 중인 상태입니다.

```bat
netstat -ano | findstr :3010
```

다른 포트로 실행해도 됩니다.

```bat
npm.cmd run dev -- -p 3011
```

### `npm error Exit handler never called!`

프로젝트 코드 오류가 아니라 npm CLI 자체 오류일 가능성이 큽니다.

해결 방법:

```bat
npm cache verify
npm.cmd run setup:web
```

그래도 반복되면 Node.js LTS 버전으로 재설치하거나 npm을 갱신한 뒤 다시 실행합니다.

### 챗봇이 로컬 대체 답변을 표시함

API가 꺼져 있거나 `.env`/GCP 인증이 안 된 상태입니다.

```text
http://localhost:8000/api/health
```

여기에서 `configured: true`, `provider: vertex`가 보여야 합니다.
